import * as React from 'react';

export interface AProps {
}

export default class A extends React.Component<AProps, any> {
  public render() {
    return (
      <div>
          A test
      </div>
    );
  }
}
